{
  "name": "gigos-mirror",
  "private": true,
  "description": "iso.gentoozh.org:Cloudflare Worker:边缘读镜像站列表渲染 Live ISO 下载落地页",
  "scripts": {
    "check": "node scripts/check.mjs",
    "preview": "node scripts/preview.mjs",
    "deploy": "npm run check && wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4"
  },
  "type": "module"
}
