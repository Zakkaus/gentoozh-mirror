# 设计语言

这一页是发行产物的落地页：读一次，按一下，走人。所以版面的主体是产物本身：文件名、大小、日期、校验和，
而不是围绕它的说明文字。

改 CSS 之前先改这里。每条规则带着它的原因；没有原因的规则守不住，下一个人会当成口味。

## 校准

**这一页做什么。** 一次性阅读，终点是一次下载。次要读者是回来核对校验和或找旧版本的人，
所以历史列表必须在同一页可达，不能藏进第二跳。

**视觉语言归谁。** 归我们。上游没有可继承的皮肤，所以每个值都要自己给出理由。

**几种语言，哪种最长。** 简体、繁体、英文。**按渲染宽度量，不按字数**：英文串少但单条最宽，
中文串宽但每条短。槽位按最长那一条留，不按平均。

**明暗。** 两套都是一等公民，从第一次提交起。深色不是后来补的一层覆盖。

**读者只有一类。** 想拿 ISO 的人。没有登录、没有权限、没有仪表板，所以没有受众轴。

## 一份文档一种语言

`/` 简体，`/zh-tw/` 繁体，`/en/` 英文。**每种语言一份完整文档，在边缘渲染好再发出去**，
不是发一份简体再用脚本换字。

原来的做法有两个代价，都不是理论上的：落地页把简体写进 HTML、由 `i18n.js` 在 body 末尾换掉，
英文读者第一屏看到的是中文；说明页把三种语言全塞进同一份文档、隐藏两份，每个读者下载三倍的正文。

换成一语言一文档之后：没有可闪动的中间态，`lang` 从第一个字节就是对的，屏幕阅读器一开口就是对的语音，
每份文档单独可缓存，`hreflang` 有实际目标可指。语言控件退化成三个链接，无脚本也能用。

代价写清楚：Worker 要按路径分发，翻译表进构建产物，`/` 的默认语言要有个决定
(按 `Accept-Language` 选，但不重定向，因为重定向会让分享出去的链接跟着读者的浏览器变化)。

## 颜色

**token 块之外零颜色字面值。** 例外要成文并附理由，目前只有一个：品牌 logo 是外部资产。

**一个 token 一次声明，用 `light-dark()` 装两套值。** 原来的写法把深色调色板写了两遍，
`:root[data-theme="dark"]` 一遍、`@media (prefers-color-scheme: dark)` 再一遍，八个 token
两份副本，改一个要记得改两处。`light-dark()` 把两套值放进同一行，`color-scheme` 决定取哪套。

**强调色只有一个色相。** `hsl(252 …)`，深浅两档由明度拉开，不引入第二个色相。

**状态色走 token，不走字面值。** 没有第二套绿/红，因为这一页没有状态。

## 焦点与键盘

**一处 `:focus-visible`，全站统一。** 2px 描边加 2px 间距，落在它所在的表面上可见。
任何控件都不许自己 `outline: none`,那是把整套约定关掉，而不是替换。

**可点击元素一律是 `<button>` 或 `<a href>`。** 复制芯片原来是 `<code role="button" tabindex="0">`，
现在是 `<button>`：Enter、Space 与焦点环都由浏览器提供，不必自写键盘处理。

## 溢出

**每个装文字的槽位都要声明行为。** 声明的方式有四种：截断、限行、换行、自己滚。没声明的算缺陷。

这一页的长串有三处，各自的决定：文件名 `word-break: break-all`（它是数据，断在哪都比溢出好）；
校验和同上；规格标签换行成第二行而不是横向滚。

## 动效

**页面预算：同时最多两个连续动画、两条缓动曲线。** 目前用了一个连续动画（锚点平滑滚动），另有两处状态过渡。

`prefers-reduced-motion` 在查询里声明动效，而不是在查询外面把时长压到零，后者会留下一个
动了但看不见的中间态。

## 镜像列表

**每一行只给目录，不给单个文件的直链。** 镜像按计划同步，可能落后几个小时，直链指向的那一版在
镜像上可能还不存在，点下去就是 404。目录里永远是该站当前实际有的几版，读者自己挑。当前版本号
写在这一段的引导句里，够他们认出要找哪一个。

**顶部的下载按钮锚到这一段，不直接下载。** 有五个来源，选哪个取决于读者在哪里，这不是页面能替
他们决定的事，所以按钮的职责是把人带到选择处。

**源站在这里用 `/gigos/` 而不是 `_raw/gigos/`。** 前者是源站自己的带壳页面，比裸目录好看；
`_raw` 只用在校验和文件的直链上。

**为此新增一个连续动画。** 锚点跳到页尾落差大，加了 `scroll-behavior: smooth`，声明在
`prefers-reduced-motion` 查询内。页面预算从零个连续动画变成一个。

## 明确不做的

- **不做毛玻璃。** 原来的顶栏是 `backdrop-filter: blur(12px) saturate(180%)`：它让底下的文字
  对比度随内容变化，量不出一个数来；在 `forced-colors` 下整层消失。换成 92% 不透明的 token
  背景加一条下边框，两个主题下都能量。
- **不做滚动出现。** 页面短，且滚动出现的起始态是隐藏，脚本失效即永久空白。
- **不做渐变按钮。** 单色 token 填充；渐变会把两个端点色带进来，其中至少一个不在 token 里。
- **不做 toast 之外的浮层。** 复制反馈是唯一的瞬时反馈，它是 `role="status"`，
  屏幕阅读器可播报；原来的 `aria-hidden="true"` 对屏幕阅读器用户没有任何反馈。
- **不做首页独有的入口。** 说明页从顶栏可达，历史版本在同一页，没有仅能从首页进入的入口。
